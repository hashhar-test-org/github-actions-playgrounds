name: ci

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - name: Simulate long process
        run: |
          for i in {1..10}; do
            echo "Iteration $i; sleeping for a minute..."
            sleep 60
          done
